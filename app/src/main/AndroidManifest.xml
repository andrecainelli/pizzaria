<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fiap.trabalho.rm78710.nac2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoggedActivity" />
        <activity android:name=".activity_logged" />
        <activity android:name=".configuracoes" />
        <activity android:name=".Calabresa" />
        <activity android:name=".Margerita" />
        <activity android:name=".Frango" />
        <activity android:name=".Mussarela"></activity>
    </application>

</manifest>